# The exact versions that produced reports/benchmark.json and
# reports/experiments.json, on the Python release named in .python-version.
#
# requirements.txt gives lower bounds, useful for opening the notebooks with
# whatever is already installed. This file reproduces the published figures to
# the last digit.
#
# The distinction is not cosmetic. The notebooks ran in 2025 under Python 3.9
# and an older scikit-learn; comparing their metrics against a script run today
# mixes two variables. The benchmark therefore fixes its environment and
# measures its gaps inside it.
#
#     pip install -r requirements-lock.txt

matplotlib==3.11.1
numpy==2.5.1
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.0
seaborn==0.13.2

# Tooling
pytest==9.1.1
ruff==0.16.0
